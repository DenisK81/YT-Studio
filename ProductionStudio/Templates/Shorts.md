# Shorts — {CASE_NAME}

Generated by Shorts Agent, immediately after the final render. Hard cap: 45 seconds per Short.
Caption styling is fixed — see "Caption style" below, matches `Tools/remotion_assembly_tool.md`.

## Short 1
**Source scenes:**
**Hook (must land in <3 seconds, not reused from the main video's Hook beat):**
**Title (max 3-5 words):**
**Description:**
**Hashtags (3-5, strongest first):**
**Target length:** ≤45s

## Short 2
**Source scenes:**
**Hook:**
**Title:**
**Description:**
**Hashtags:**
**Target length:** ≤45s

(repeat per Short, up to `max_shorts`)

## Caption style (all Shorts — fixed, do not restyle per-video)
Bold Montserrat Bold (reuses the channel's existing body font — see
`Config/config.schema.json`), white text with a thin black stroke, word-by-word karaoke-style
highlight (active word in the channel's accent color #A30E15, others dimmer/smaller), displayed
in 1-word or 2-5 word phrases, centered in the lower third of frame, each phrase on screen for
at least 2 seconds.
